import { MODULE_ID } from "./constants.js";

/**
 * Register module settings. Called during the `init` hook.
 * Add `game.settings.register(MODULE_ID, ...)` calls here.
 */
export function registerSettings() {
  // Example (uncomment and customize as needed):
  // game.settings.register(MODULE_ID, "exampleSetting", {
  //   name: `${MODULE_ID}.settings.exampleSetting.name`,
  //   hint: `${MODULE_ID}.settings.exampleSetting.hint`,
  //   scope: "world",
  //   config: true,
  //   type: Boolean,
  //   default: false,
  // });
}
